[project]
name = "pdf-analyzer"
version = "0.1.0"
description = "Flask web app to detect real vs modified PDFs via metadata analysis"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "pypdf>=6.14.2",
    "werkzeug>=3.1.8",
    "python-dotenv>=1.1.0",
]
